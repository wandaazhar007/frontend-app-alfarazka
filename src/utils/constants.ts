// Page size for ALL paginated tables/lists (docs/07_DESIGN_SYSTEM.md §8.11).
// Change to 10 before production — 3 is used during development to make pagination easy to test.
export const PAGE_SIZE = 3;
