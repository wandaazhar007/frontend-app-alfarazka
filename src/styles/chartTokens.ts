// Recharts needs color values as JS strings — it can't read SCSS directly.
// MUST be kept manually in sync with styles/_variables.scss if the color tokens change.

export const chartTokens = {
  primary: '#e63946',
  primarySoft: 'rgba(230, 53, 70, 0.15)',
  border: '#e5e7eb',
  textMuted: '#6b7280',
  surface: '#ffffff',
};
